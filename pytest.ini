[pytest]
# Default pytest matches the portable public tier.
# generated/ and real_world/ need extra fixtures and are not the clone check.
testpaths = tests
norecursedirs = generated real_world
addopts = --ignore=tests/test_failloud.py
